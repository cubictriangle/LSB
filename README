
This is Lua 5.4.7, released on 13 Jun 2024.

For installation instructions, license details, and
further information about Lua, see doc/readme.html.

===========================
Lua libraries `os`, `io` and `debug` are removed for security concern.
Lua library `package` has been modified, the `cpath` and `loadlib` are removed. Loading C libraries is riskly, users should only be able to load Lua libraries.